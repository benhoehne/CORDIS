"""
Web + export layer (Task 3).

app        : FastAPI application (JSON API + static frontend + export route).
repository : data-access layer – all SQLite queries against heidelberg_projects.
export     : build filtered Excel exports into the data/ folder.
"""
